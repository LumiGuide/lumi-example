# This expression defines the jobs that hydra.lumi.guide will build.
let
  pkgs = import ./default.nix;
in {

  # In practise we have lots of Hydra jobs defined here. As in:
  # somePackage = pkgs.somePackage

}
